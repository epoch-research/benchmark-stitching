This folder looks at the results of modeling ECI (per outputs/model_fit/model_capabilities.csv) as a linear function of release date and log(training compute) (taken from data/all_ai_models.csv).

Any outputs are saved to outputs/algorithmic_progress_methods/linear_model/